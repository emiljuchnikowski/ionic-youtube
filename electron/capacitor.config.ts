import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.testowe',
  appName: 'Testowa Ionica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
